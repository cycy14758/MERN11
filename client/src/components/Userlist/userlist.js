import React from 'react'
  
function Userlist() {
  return (
    <div>
      <h2>User List</h2>
      <ul>
        {/*users.map((user) => (
          <li key={user.id}>
            <strong>{user.name}</strong>
            <p>Email: {user.email}</p>
          </li>
      ))
        */}
      </ul>
    </div>
  )
}

export default Userlist